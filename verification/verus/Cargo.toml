[package]
name = "lambars-verus-verification"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
vstd = "=0.0.0-2026-09-20-0158"

[package.metadata.verus]
verify = true

[lints.rust]
unsafe_code = "deny"
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(verus_only)'] }
